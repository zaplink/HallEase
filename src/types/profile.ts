export type Profile = {
	id: string;
	full_name: string;
	pro_pic: string;
};

export type ProfileWithRole = {
	id: string;
	full_name: string;
	role: string;
	pro_pic: string;
	email: string;
	position: string;
};
